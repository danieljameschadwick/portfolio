type Card {
    _id: String!
    cardType: String!
    title: String!
    description: String!
    displayOrder: Int!
    subTitle: String
    imagePath: String
    technologies: [String]
}

type Query {
    card(_id: String!): Card,
    cards(cardType: String): [Card],
}
